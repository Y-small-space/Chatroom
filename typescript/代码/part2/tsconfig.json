{
    /*tsconfig.json是ts编译器的配置文件*/
    // "include" 用来指定哪些 ts 文件需要被编译
    // ** 表示任意目录
    // * 表示任意文件

    "include": [
        "/**/*"
    ],  
   
    "exclude": [
        "./node_modules"
    ],
      
    /*
        compilerOptions 编译器的选项
    */
    "compilerOptions": {
        // target 用来指定ts被编译为的ES的版本
        "target": "ESNext",
        "module": "ES2015",
        "lib": ["ES6","DOM"],

        // outDir 用来指定编译后文件所在目录
        // 将代码合并为一个文件
        // 设置outFile后，所有的全局作用域中的代码都会合并到同一个文件中 去
        "outDir": "./src",
        
        // 是否对JS文件进行编译，默认是false
        "allowJs": false,

        // 是否检查JS代码是否符合语法规范，默认值是false
        "checkJs": false,

        // 是否移除注释
        "removeComments": false ,

        // 不生成编译后的文件
        "noEmit": true,

        // 有错误时不生成编译后的文件
        "noEmitOnError": false,

        // 用来设置编译后的文件是否使用严格模式，默认为false
        "alwaysStrict": false,

        // 不允许隐式的any类型
        "noImplicitAny": false,

        // 不允许不明确类型的this
        "noImplicitThis": false,

        // 严格检查空值
        "strictNullChecks": false,

        // 所有严格检查的总开关
        "strict": false
    },
     
    
}
